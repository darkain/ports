--- modules/freebsd/vmmemctl/Makefile.orig	2021-01-13 22:58:07 UTC
+++ modules/freebsd/vmmemctl/Makefile
@@ -28,13 +28,13 @@ SRCS	+= kernelStubsBSD.c
 
 .if $(MACHINE_ARCH) == "amd64"
 SRCS    += backdoorGcc64.c
+.elif $(MACHINE_ARCH) == "aarch64"
+SRCS    += backdoorGcc64_arm64.c
 .else
 SRCS    += backdoorGcc32.c
 .endif
 KMOD	= vmmemctl
 PROG    = ../$(KMOD).ko
-NOMAN	= t
-NO_MAN  = t
 KLDMOD	= t
 
 # Don't print a warning that the object dir wasn't specified
